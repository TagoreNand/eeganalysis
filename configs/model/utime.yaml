# U-Time-style multi-scale temporal U-Net over epoch embeddings.
name: utime
emb_dim: 128
base: 64
depth: 3
pool: 2
sfreq: 100
lr: 1.0e-3
weight_decay: 1.0e-3
max_epochs: 150
use_focal: false
focal_gamma: 2.0
pretrained_encoder: null
