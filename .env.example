# Copy to .env and fill in. Never commit the real .env.
MLFLOW_TRACKING_URI=http://localhost:5000
WANDB_API_KEY=
WANDB_PROJECT=eegpipe
# Remote storage for DVC (example: S3 / MinIO)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
DVC_REMOTE_URL=s3://my-eeg-bucket/dvcstore
