apiVersion: v1
kind: Service
metadata:
  name: eegpipe
spec:
  selector: { app: eegpipe }
  ports:
    - { name: http, port: 80, targetPort: 8000 }
  type: ClusterIP            # front with an Ingress/LoadBalancer for external access
